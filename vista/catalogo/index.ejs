<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Tienda Virtual</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/css/energetic.css" rel="stylesheet">
</head>
<body>
    <!-- Navegación -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-store"></i> Tienda Virtual MVC
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">
                            <i class="fas fa-tachometer-alt"></i> Dashboard
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/categorias">
                            <i class="fas fa-tags"></i> Categorías
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/productos">
                            <i class="fas fa-box"></i> Productos
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/clientes">
                            <i class="fas fa-users"></i> Clientes
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/ventas">
                            <i class="fas fa-shopping-cart"></i> Ventas
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/catalogo">
                            <i class="fas fa-book"></i> Catálogo
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Contenido principal -->
    <main class="container mt-4">
        <!-- Header -->
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <h1 class="h2"><i class="fas fa-book text-primary"></i> <%= title %></h1>
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a href="/">Dashboard</a></li>
                        <li class="breadcrumb-item active">Catálogo</li>
                    </ol>
                </nav>
                <p class="text-muted">Selecciona un cliente para enviarle productos por WhatsApp</p>
            </div>
        </div>

        <!-- Buscador -->
        <div class="row mb-4">
            <div class="col-md-6">
                <form method="GET" action="/catalogo">
                    <div class="input-group">
                        <input type="text" 
                               class="form-control" 
                               name="search" 
                               value="<%= search %>"
                               placeholder="Buscar cliente por nombre, celular o correo...">
                        <button class="btn btn-outline-primary" type="submit">
                            <i class="fas fa-search"></i> Buscar
                        </button>
                        <% if (search) { %>
                            <a href="/catalogo" class="btn btn-outline-secondary">
                                <i class="fas fa-times"></i> Limpiar
                            </a>
                        <% } %>
                    </div>
                </form>
            </div>
        </div>

        <!-- Estadísticas rápidas -->
        <div class="row mb-4">
            <div class="col-md-4">
                <div class="card bg-info text-white">
                    <div class="card-body">
                        <div class="d-flex justify-content-between">
                            <div>
                                <h6 class="card-title">Total Clientes</h6>
                                <h3 class="mb-0"><%= clientes.length %></h3>
                            </div>
                            <div class="align-self-center">
                                <i class="fas fa-users fa-2x"></i>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card bg-success text-white">
                    <div class="card-body">
                        <div class="d-flex justify-content-between">
                            <div>
                                <h6 class="card-title">Con WhatsApp</h6>
                                <h3 class="mb-0"><%= clientes.filter(c => c.celular).length %></h3>
                            </div>
                            <div class="align-self-center">
                                <i class="fab fa-whatsapp fa-2x"></i>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card bg-warning text-white">
                    <div class="card-body">
                        <div class="d-flex justify-content-between">
                            <div>
                                <h6 class="card-title">Clientes Activos</h6>
                                <h3 class="mb-0"><%= clientes.filter(c => c.total_compras > 0).length %></h3>
                            </div>
                            <div class="align-self-center">
                                <i class="fas fa-user-check fa-2x"></i>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Lista de clientes -->
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0">
                    <i class="fas fa-users"></i> Seleccionar Cliente para Envío de Catálogo
                    <% if (search) { %>
                        <small class="text-muted">- Resultados para: "<%= search %>"</small>
                    <% } %>
                </h5>
            </div>
            <div class="card-body">
                <% if (clientes.length === 0) { %>
                    <div class="text-center py-5">
                        <i class="fas fa-users fa-3x text-muted mb-3"></i>
                        <% if (search) { %>
                            <h5 class="text-muted">No se encontraron clientes</h5>
                            <p class="text-muted">No hay clientes que coincidan con tu búsqueda</p>
                            <a href="/catalogo" class="btn btn-primary">
                                <i class="fas fa-list"></i> Ver Todos los Clientes
                            </a>
                        <% } else { %>
                            <h5 class="text-muted">No hay clientes registrados</h5>
                            <p class="text-muted">Registra clientes para poder enviarles el catálogo</p>
                            <a href="/clientes/crear" class="btn btn-primary">
                                <i class="fas fa-plus"></i> Registrar Primer Cliente
                            </a>
                        <% } %>
                    </div>
                <% } else { %>
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead class="table-dark">
                                <tr>
                                    <th>Cliente</th>
                                    <th>Contacto</th>
                                    <th>Historial</th>
                                    <th>Última Compra</th>
                                    <th>Acción</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% clientes.forEach(cliente => { %>
                                    <tr>
                                        <td>
                                            <div class="d-flex align-items-center">
                                                <div class="avatar bg-primary text-white rounded-circle me-2 d-flex align-items-center justify-content-center" 
                                                     style="width: 40px; height: 40px;">
                                                    <%= cliente.nombre.charAt(0).toUpperCase() %>
                                                </div>
                                                <div>
                                                    <h6 class="mb-0"><%= cliente.nombre %></h6>
                                                    <small class="text-muted">ID: #<%= cliente.id %></small>
                                                </div>
                                            </div>
                                        </td>
                                        <td>
                                            <% if (cliente.celular) { %>
                                                <div>
                                                    <i class="fab fa-whatsapp text-success"></i> 
                                                    <strong><%= cliente.celular %></strong>
                                                </div>
                                            <% } else { %>
                                                <div class="text-warning">
                                                    <i class="fas fa-exclamation-triangle"></i> Sin celular
                                                </div>
                                            <% } %>
                                            <% if (cliente.correo) { %>
                                                <div>
                                                    <i class="fas fa-envelope text-info"></i> 
                                                    <%= cliente.correo %>
                                                </div>
                                            <% } %>
                                        </td>
                                        <td>
                                            <% if (cliente.total_compras && cliente.total_compras > 0) { %>
                                                <span class="badge bg-success">
                                                    <%= cliente.total_compras %> compras
                                                </span>
                                                <br>
                                                <small class="text-muted">$<%= parseFloat(cliente.total_gastado || 0).toFixed(2) %> gastado</small>
                                            <% } else { %>
                                                <span class="badge bg-secondary">Sin compras</span>
                                            <% } %>
                                        </td>
                                        <td>
                                            <% if (cliente.ultima_compra) { %>
                                                <small>
                                                    <%= new Date(cliente.ultima_compra).toLocaleDateString('es-ES') %>
                                                </small>
                                            <% } else { %>
                                                <small class="text-muted">Nunca</small>
                                            <% } %>
                                        </td>
                                        <td>
                                            <% if (cliente.celular) { %>
                                                <a href="/catalogo/productos/<%= cliente.id %>" 
                                                   class="btn btn-success btn-sm" 
                                                   title="Enviar catálogo por WhatsApp">
                                                    <i class="fab fa-whatsapp"></i> Enviar Catálogo
                                                </a>
                                            <% } else { %>
                                                <button class="btn btn-secondary btn-sm" 
                                                        disabled 
                                                        title="Cliente sin número de WhatsApp">
                                                    <i class="fas fa-ban"></i> Sin WhatsApp
                                                </button>
                                            <% } %>
                                        </td>
                                    </tr>
                                <% }); %>
                            </tbody>
                        </table>
                    </div>
                <% } %>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-dark text-white text-center py-3 mt-5">
        <div class="container">
            <p>&copy; <%= new Date().getFullYear() %> Tienda Virtual MVC - Catálogo de Productos</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/motivational-effects.js"></script>
</body>
</html>