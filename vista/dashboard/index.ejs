<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Tienda Virtual</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/css/energetic.css" rel="stylesheet">
</head>
<body>
    <!-- Navegación -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-store"></i> Tienda Virtual MVC
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link active" href="/">
                            <i class="fas fa-tachometer-alt"></i> Dashboard
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/categorias">
                            <i class="fas fa-tags"></i> Categorías
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/productos">
                            <i class="fas fa-box"></i> Productos
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/clientes">
                            <i class="fas fa-users"></i> Clientes
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/ventas">
                            <i class="fas fa-shopping-cart"></i> Ventas
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/catalogo">
                            <i class="fas fa-book"></i> Catálogo
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Contenido principal -->
    <main class="container mt-4">
        <!-- Header -->
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <h1 class="h2">
                    <i class="fas fa-tachometer-alt text-primary"></i> <%= title %>
                </h1>
                <p class="text-muted mb-0">Bienvenido al sistema de gestión de tienda virtual</p>
            </div>
            <div class="text-end">
                <small class="text-muted">
                    <i class="fas fa-calendar-alt"></i> 
                    <%= new Date().toLocaleDateString('es-ES', { 
                        weekday: 'long', 
                        year: 'numeric', 
                        month: 'long', 
                        day: 'numeric' 
                    }) %>
                </small>
            </div>
        </div>

        <!-- Estadísticas principales -->
        <div class="row mb-4">
            <div class="col-xl-3 col-md-6 mb-4">
                <div class="card border-0 shadow-sm h-100">
                    <div class="card-body text-center">
                        <div class="d-flex align-items-center justify-content-center mb-2">
                            <div class="rounded-circle bg-primary bg-opacity-10 p-3">
                                <i class="fas fa-tags fa-2x text-primary"></i>
                            </div>
                        </div>
                        <h3 class="card-title h4 mb-1"><%= stats.totalCategorias %></h3>
                        <p class="card-text text-muted mb-0">Total Categorías</p>
                        <div class="mt-2">
                            <a href="/categorias" class="btn btn-sm btn-outline-primary">
                                <i class="fas fa-eye"></i> Ver
                            </a>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-xl-3 col-md-6 mb-4">
                <div class="card border-0 shadow-sm h-100">
                    <div class="card-body text-center">
                        <div class="d-flex align-items-center justify-content-center mb-2">
                            <div class="rounded-circle bg-success bg-opacity-10 p-3">
                                <i class="fas fa-box fa-2x text-success"></i>
                            </div>
                        </div>
                        <h3 class="card-title h4 mb-1"><%= stats.totalProductos %></h3>
                        <p class="card-text text-muted mb-0">Total Productos</p>
                        <% if (stats.productosPocoStock > 0) { %>
                        <div class="mt-2">
                            <span class="badge bg-warning"><%= stats.productosPocoStock %> con poco stock</span>
                        </div>
                        <% } %>
                        <div class="mt-2">
                            <a href="/productos" class="btn btn-sm btn-outline-success">
                                <i class="fas fa-eye"></i> Ver
                            </a>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-xl-3 col-md-6 mb-4">
                <div class="card border-0 shadow-sm h-100">
                    <div class="card-body text-center">
                        <div class="d-flex align-items-center justify-content-center mb-2">
                            <div class="rounded-circle bg-info bg-opacity-10 p-3">
                                <i class="fas fa-users fa-2x text-info"></i>
                            </div>
                        </div>
                        <h3 class="card-title h4 mb-1"><%= stats.totalClientes %></h3>
                        <p class="card-text text-muted mb-0">Total Clientes</p>
                        <div class="mt-2">
                            <a href="/clientes" class="btn btn-sm btn-outline-info">
                                <i class="fas fa-eye"></i> Ver
                            </a>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-xl-3 col-md-6 mb-4">
                <div class="card border-0 shadow-sm h-100">
                    <div class="card-body text-center">
                        <div class="d-flex align-items-center justify-content-center mb-2">
                            <div class="rounded-circle bg-warning bg-opacity-10 p-3">
                                <i class="fas fa-shopping-cart fa-2x text-warning"></i>
                            </div>
                        </div>
                        <h3 class="card-title h4 mb-1"><%= stats.totalVentas %></h3>
                        <p class="card-text text-muted mb-0">Total Ventas</p>
                        <% if (stats.ventasPendientes > 0) { %>
                        <div class="mt-2">
                            <span class="badge bg-warning"><%= stats.ventasPendientes %> pendientes</span>
                        </div>
                        <% } %>
                        <div class="mt-2">
                            <a href="/ventas" class="btn btn-sm btn-outline-warning">
                                <i class="fas fa-eye"></i> Ver
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Estadísticas del día y alertas -->
        <div class="row mb-4">
            <div class="col-lg-4">
                <div class="card border-0 shadow-sm h-100">
                    <div class="card-header bg-success text-white">
                        <h6 class="mb-0">
                            <i class="fas fa-chart-line"></i> Ventas de Hoy
                        </h6>
                    </div>
                    <div class="card-body text-center">
                        <div class="row">
                            <div class="col-6">
                                <h4 class="text-success"><%= stats.ventasHoy.count %></h4>
                                <p class="text-muted mb-0 small">Ventas realizadas</p>
                            </div>
                            <div class="col-6">
                                <h4 class="text-success">$<%= stats.ventasHoy.total.toLocaleString() %></h4>
                                <p class="text-muted mb-0 small">Total vendido</p>
                            </div>
                        </div>
                        <div class="mt-3">
                            <a href="/ventas/crear" class="btn btn-success btn-sm">
                                <i class="fas fa-plus"></i> Nueva Venta
                            </a>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-lg-4">
                <div class="card border-0 shadow-sm h-100">
                    <div class="card-header bg-info text-white">
                        <h6 class="mb-0">
                            <i class="fas fa-calendar-alt"></i> Estadísticas del Mes
                        </h6>
                    </div>
                    <div class="card-body text-center">
                        <div class="row">
                            <div class="col-6">
                                <h4 class="text-info"><%= stats.ventasMes || 0 %></h4>
                                <p class="text-muted mb-0 small">Ventas del mes</p>
                            </div>
                            <div class="col-6">
                                <h4 class="text-info">$<%= (stats.ingresosMes || 0).toLocaleString() %></h4>
                                <p class="text-muted mb-0 small">Ingresos del mes</p>
                            </div>
                        </div>
                        <div class="mt-3">
                            <a href="/ventas" class="btn btn-info btn-sm">
                                <i class="fas fa-chart-bar"></i> Ver Reportes
                            </a>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-lg-4">
                <div class="card border-0 shadow-sm h-100">
                    <div class="card-header bg-<%= stats.productosPocoStock > 0 ? 'warning' : 'success' %> text-white">
                        <h6 class="mb-0">
                            <i class="fas fa-<%= stats.productosPocoStock > 0 ? 'exclamation-triangle' : 'check-circle' %>"></i> 
                            Estado del Inventario
                        </h6>
                    </div>
                    <div class="card-body text-center">
                        <% if (stats.productosPocoStock > 0) { %>
                            <h4 class="text-warning"><%= stats.productosPocoStock %></h4>
                            <p class="text-muted mb-0 small">Productos con poco stock</p>
                            <div class="alert alert-warning mt-3 mb-3 py-2">
                                <small><i class="fas fa-exclamation-triangle"></i> Productos ≤ 5 unidades</small>
                            </div>
                            <a href="/productos?categoria=todas&buscar=" class="btn btn-warning btn-sm">
                                <i class="fas fa-boxes"></i> Revisar Stock
                            </a>
                        <% } else { %>
                            <h4 class="text-success"><i class="fas fa-check-circle"></i></h4>
                            <p class="text-muted mb-0 small">Inventario en buen estado</p>
                            <div class="alert alert-success mt-3 mb-3 py-2">
                                <small><i class="fas fa-check"></i> Stock suficiente en todos los productos</small>
                            </div>
                            <a href="/productos" class="btn btn-success btn-sm">
                                <i class="fas fa-eye"></i> Ver Productos
                            </a>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>

        <!-- Últimas ventas -->
        <div class="row">
            <div class="col-12">
                <div class="card shadow">
                    <div class="card-header py-3">
                        <h6 class="m-0 font-weight-bold text-primary">
                            <i class="fas fa-list"></i> Últimas Ventas
                        </h6>
                    </div>
                    <div class="card-body">
                        <% if (stats.ultimasVentas.length === 0) { %>
                            <div class="text-center py-4">
                                <i class="fas fa-shopping-cart fa-3x text-muted mb-3"></i>
                                <h6 class="text-muted">No hay ventas registradas</h6>
                                <p class="text-muted">Las ventas aparecerán aquí cuando se registren</p>
                            </div>
                        <% } else { %>
                            <div class="table-responsive">
                                <table class="table table-bordered">
                                    <thead class="table-light">
                                        <tr>
                                            <th>ID</th>
                                            <th>Cliente</th>
                                            <th>Fecha</th>
                                            <th>Total</th>
                                            <th>Estado</th>
                                            <th>Acciones</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% stats.ultimasVentas.forEach(venta => { %>
                                            <tr>
                                                <td>#<%= venta.id %></td>
                                                <td>
                                                    <%= venta.cliente_nombre || 'Cliente no especificado' %>
                                                </td>
                                                <td>
                                                    <%= new Date(venta.fecha).toLocaleDateString('es-ES') %>
                                                </td>
                                                <td>
                                                    $<%= parseFloat(venta.total_a_pagar).toFixed(2) %>
                                                </td>
                                                <td>
                                                    <% if (venta.estado_pago === 'pagado') { %>
                                                        <span class="badge bg-success">Pagado</span>
                                                    <% } else { %>
                                                        <span class="badge bg-warning">Pendiente</span>
                                                    <% } %>
                                                </td>
                                                <td>
                                                    <a href="/ventas/<%= venta.id %>" class="btn btn-sm btn-outline-info">
                                                        <i class="fas fa-eye"></i>
                                                    </a>
                                                </td>
                                            </tr>
                                        <% }); %>
                                    </tbody>
                                </table>
                            </div>
                            <div class="text-end">
                                <a href="/ventas" class="btn btn-primary">
                                    <i class="fas fa-list"></i> Ver Todas las Ventas
                                </a>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-dark text-white text-center py-3 mt-5">
        <div class="container">
            <p>&copy; <%= new Date().getFullYear() %> Tienda Virtual MVC - Proyecto Académico</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/motivational-effects.js"></script>

    <style>
        .border-left-primary {
            border-left: .25rem solid #4e73df!important;
        }
        .border-left-success {
            border-left: .25rem solid #1cc88a!important;
        }
        .border-left-info {
            border-left: .25rem solid #36b9cc!important;
        }
        .border-left-warning {
            border-left: .25rem solid #f6c23e!important;
        }
    </style>
</body>
</html>